<div class="product-page">
  <a href="/">Voltar</a>
  <h1>{{product.name}}</h1>
  <p>Preço: R${{product.price}}</p>
  <img src="{{product.image}}" alt="{{product.name}}" />
  <p>{{product.description}}</p>
  <form method="POST" action="/remove/{{product._id}}">
    <input class="btn" type="submit" value="Excluir" />
  </form>
</div>